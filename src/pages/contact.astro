---
import Base from "@/components/layout/Base.astro";
import Button from "@/components/store-ui/Button";
import { Input } from "@/components/store-ui/Input";
import { Textarea } from "@/components/store-ui/Textarea";
import MaxWidthContainer from "@/components/layout/MaxWidthContainer.astro";

---

<Base title="Get In Touch | SVG Piggies">
  <MaxWidthContainer>
    <section>
      <div class="flex flex-col items-center gap-[32px]">
        <div class="flex items-center gap-[1rem]">
          <div class="bg-primary h-[1px] w-[124px]"></div>
          <div class="text-2xl font-bold">CONTACT US</div>
          <div class="bg-primary h-[1px] w-[124px]"></div>
        </div>

        <div class="mb-8 text-center w-full">
          <h1 class="text-3xl font-semibold tracking-tight sm:text-4xl">
            We'd love to hear from you!
          </h1>
          <p id="contact-description" class="mt-3 text-muted-foreground">
            Have a question or need help? Send us a message and we'll get back to
            you as soon as possible.
          </p>
        </div>

        <form
          method="POST"
          aria-describedby="contact-description"
          class="flex flex-col gap-y-6 w-full"
        >
          <div class="grid gap-6 sm:grid-cols-2">
            <div class="flex flex-col gap-y-2">
              <label for="first-name" class="flex items-center gap-2 text-lg leading-none font-medium select-none">
                First Name <span aria-hidden="true" class="text-xs text-red-500">*</span>
              </label>
              <Input
                id="first-name"
                name="firstName"
                type="text"
                autoComplete="given-name"
                placeholder="First name"
                className="text-[16px] md:text-[18px]"
                required
              />
            </div>

            <div class="flex flex-col gap-y-2">
              <label for="last-name" class="flex items-center gap-2 text-lg leading-none font-medium select-none">
                Last Name
              </label>
              <Input
                id="last-name"
                name="lastName"
                type="text"
                autoComplete="family-name"
                placeholder="Last name"
                className="text-[16px] md:text-[18px]"
              />
            </div>
          </div>

          <div class="flex flex-col gap-y-2">
            <label for="email" class="flex items-center gap-2 text-lg leading-none font-medium select-none">
              Email <span aria-hidden="true" class="text-xs text-red-500">*</span>
            </label>
            <Input
              id="email"
              name="email"
              type="email"
              autoComplete="email"
              inputMode="email"
              placeholder="Email"
              className="text-[16px] md:text-[18px]"
              required
            />
          </div>

          <div class="flex flex-col gap-y-2">
            <label for="message" class="flex items-center gap-2 text-lg leading-none font-medium select-none">
              Message <span aria-hidden="true" class="text-xs text-red-500">*</span>
            </label>
            <Textarea
              id="message"
              name="message"
              rows={8}
              minLength={10}
              placeholder="How can we help you?"
              className="text-[16px] md:text-[18px]"
              required
            />
          </div>

          <div class="flex justify-center">
            <Button className="!px-10">Send</Button>
          </div>
        </form>
      </div>
    </section>
  </MaxWidthContainer>
</Base>
