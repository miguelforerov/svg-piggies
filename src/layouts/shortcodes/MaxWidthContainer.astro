---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div class:list={`flex flex-col max-w-[82rem] px-4 lg:px-8 items-center gap-16 my-4 w-full ${className}`}>
  <slot />
</div>
