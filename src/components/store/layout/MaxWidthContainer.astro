---
interface Props {
  className?: string;
  gap?: string;
}

const { className, gap = "gap-16" } = Astro.props;
---

<div
  class:list={[
    "flex flex-col max-w-[82rem] items-center my-4 px-4 lg:px-8 w-full",
    gap,
    className,
  ]}
>
  <slot />
</div>
